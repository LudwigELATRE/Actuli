{% extends "base.html.twig" %}


{% block content %}
    <!-- Page content-->
    <section class="vh-100">
        <div class="container py-5 h-100">

{#            {% if error %}
                <div class="alert alert-danger" role="alert">
                    {{ error }}
                </div>
            {% endif %}#}
            <div class="row d-flex align-items-center justify-content-center h-100">
                <div class="col-md-8 col-lg-7 col-xl-6">
                    {% if success %}
                        <div class="alert alert-success" role="alert">
                            {{ success }}
                        </div>
                    {% endif %}
                    <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/draw2.svg"
                         class="img-fluid" alt="Phone image">
                </div>
                <div class="col-md-7 col-lg-5 col-xl-5 offset-xl-1">
                    <form action="/login" method="post">
                        <!-- Email input -->
                        <div class="form-outline mb-4">
                            <input type="email" id="form1Example13" class="form-control form-control-lg" id="email" name="email" placeholder="Enter your email"/>
                            <label class="form-label" for="form1Example13">Votre Email</label>
                            <div class="valid-feedback">
                        </div>

                        <!-- Password input -->
                        <div class="form-outline mb-4">
                            <input type="password" id="form1Example23" class="form-control form-control-lg" id="password" name="password" placeholder="Enter your password"/>
                            <label class="form-label" for="form1Example23">Votre mot de passe</label>
                            <div class="valid-feedback">
                        </div>

{#                        <div class="d-flex justify-content-around align-items-center mb-4">
                            <!-- Checkbox -->
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" value="" id="form1Example3" checked />
                                <label class="form-check-label" for="form1Example3"> Remember me </label>
                            </div>
                            <a href="#!">Forgot password?</a>
                        </div>#}


                        <div class="d-flex justify-content-around align-items-center mb-4">
                            <!-- Checkbox -->
                            <b>Si vous n'avez pas de compte, cliquez sur <a href="/register">ici</a> ?</b>
                        </div>
                        <!-- Submit button -->
                        <button type="submit" class="btn btn-primary btn-lg btn-block">Se connecter</button>
                    </form>
                </div>
            </div>
        </div>
    </section>
{% endblock %}